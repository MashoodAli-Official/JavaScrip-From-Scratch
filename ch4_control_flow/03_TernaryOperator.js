//👇👇 Ternary operator in javascript

// The conditional operator, also referred to as the ternary operator (?:), 
// is a shortcut for expressing conditional statements in JavaScript.

//SYTEX OF TERNARY (?) OPERATOR 
//condition ? value if true : value if false

var num = 5;
var result = num >= 8 ? 'Greater than 8' : 'Smaller then 8 ';
console.log(`The number ${num} is ${result}`);

//Using backticks and ${} makes it straightforward to insert variables directly into a string. 
//This makes it simpler to include dynamic values in strings without having to concatenate parts using the plus (+) operator.