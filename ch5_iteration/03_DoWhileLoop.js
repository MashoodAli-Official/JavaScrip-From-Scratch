// 👇👇 DO WHILE LOOP
// In JavaScript, a do...while loop is a control flow statement that executes a 
// block of code at least once, and then repeats the execution as long as a 
// specified condition evaluates to true. 

var i = 0;
do {
    console.log('This is Do While Loop');
    i++;
} while (i < 5);

//In this 👆👆 example.
//In this example, it will print numbers from 0 to 4 because the loop stops 
// when i becomes equal to 5. Even if the condition (i < 5) evaluates to false after 
// the first iteration, the loop will still run at least once because it's a do...while loop.